services:
  postgres:
    image: postgres:16
    restart: "no"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: users_billing
    ports:
      - "5432:5432"
    volumes:
      - users_billing_pgdata:/var/lib/postgresql/data

volumes:
  users_billing_pgdata:
