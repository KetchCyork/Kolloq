# Sidecar build outputs — regenerated by sidecar/build.mjs on every build (see beforeBuildCommand).
# The staged Node runtime is a large platform-specific binary and must never be committed.
sidecar/dist/
sidecar/runtime/
sidecar/.node-cache/
