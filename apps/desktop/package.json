{
  "name": "@newvector/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/ensure-target-unindexed.sh && tauri dev",
    "build": "bash scripts/ensure-target-unindexed.sh && tauri build",
    "qa-build": "bash scripts/qa-build.sh",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
